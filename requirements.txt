# Minimal requirements for Streamlit Cloud (1 GB RAM limit)
# Use TensorFlow Lite models with minimal TensorFlow footprint

# TensorFlow with only CPU support (avoids large GPU libraries)
# This is much lighter than full tensorflow  (single wheel, not multi-file)
tensorflow-cpu==2.15.0

# Image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0
numpy>=1.24.0,<2.0

# Web UI
streamlit>=1.28.0
streamlit-option-menu>=0.3.6

# Data science
pandas>=2.0.0
scikit-learn>=1.3.0
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Model download from HF Hub
huggingface_hub>=0.23.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
